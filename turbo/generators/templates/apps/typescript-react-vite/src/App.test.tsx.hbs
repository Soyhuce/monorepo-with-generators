import { render } from '@testing-library/react'

import { App } from './App'

describe('App', () => {
  it('should match snapshot for the App component', () => {
    const component = render(<App />)
    expect(component).toMatchSnapshot()
  })
})
