import { render } from '@testing-library/react'

import { \{{ pascalCase name }} } from './{{ pascalCase nnameame }}'

describe('\{{ sentenceCase type }} / \{{ pascalCase name }}', () => {
  it('should match snapshot for the \{{ pascalCase name }} component', () => {
    const component = render(<\{{ pascalCase name }} />)
    expect(component).toMatchSnapshot()
  })
})
